<%
  # Preparamos las props para nuestro componente React
  react_props = {
    i18n: t('devise.sessions.new'),
    csrfToken: form_authenticity_token,
    signUpPath: new_registration_path(resource_name),
    forgotPasswordPath: new_password_path(resource_name)
  }
%>

<%= react_component("LogIn", props: react_props) %>